# Values before a section belong to the root configuration.
name = checkout-api
port = 8081 # comments can follow values
timeout = 5s

[global] # This section also writes to the root configuration.
debug = true
